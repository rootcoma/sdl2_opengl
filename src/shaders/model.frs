#version 330

uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;

in vec4 pos;
varying vec3 normal;

out vec4 fragColor;

void main()
{
    fragColor = vec4((sin(pos.x + gl_FragCoord.x/40.0) + 1)/2, (sin(pos.y+gl_FragCoord.y/20.0) + 1)/2, (sin(pos.y-gl_FragCoord.z/30.0)+1)/2, 1.0f);
}
